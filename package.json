{
  "name": "trading212-controlled-mcp",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "description": "Trading 212 MCP server with controlled two-step execution",
  "engines": { "node": ">=20" },
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test",
    "check": "node --check src/client.js && node --check src/server.js",
    "build:claude": "npx @anthropic-ai/mcpb@latest validate manifest.json && npx @anthropic-ai/mcpb@latest pack"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.25.2",
    "dotenv": "^16.6.1",
    "zod": "^3.24.2"
  }
}
